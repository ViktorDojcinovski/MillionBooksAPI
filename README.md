This is a very simple static server created with http-server npm package.

## Available Scripts

In the project directory, you can run:

### `http-server library.json`

to start the server at http://localhost:8080
